<Window
    x:Class="Floozys_Hotel.Views.NewGuestView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Guest"
    Width="400"
    Height="400"
    WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock
            Margin="0,0,0,20"
            FontSize="20"
            FontWeight="Bold"
            Text="{Binding WindowTitle}" />
        <StackPanel Grid.Row="1" VerticalAlignment="Top">
            <TextBlock
                Margin="0,0,0,10"
                FontStyle="Italic"
                Foreground="Gray"
                Text="Fields marked with * are required." />
            <StackPanel Margin="0,5" Orientation="Horizontal">
                <TextBlock Width="100"><Run Text="First name" /><Run Foreground="Red" Text="*" /></TextBlock>
                <TextBox Width="200" Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <StackPanel Margin="0,5" Orientation="Horizontal">
                <TextBlock Width="100"><Run Text="Last name" /><Run Foreground="Red" Text="*" /></TextBlock>
                <TextBox Width="200" Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <StackPanel Margin="0,5" Orientation="Horizontal">
                <TextBlock Width="100"><Run Text="Phone" /><Run Foreground="Red" Text="*" /></TextBlock>
                <TextBox Width="200" Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <StackPanel Margin="0,5" Orientation="Horizontal">
                <TextBlock Width="100"><Run Text="Email" /><Run Foreground="Red" Text="*" /></TextBlock>
                <TextBox Width="200" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <StackPanel Margin="0,5" Orientation="Horizontal">
                <TextBlock Width="100"><Run Text="Country" /><Run Foreground="Red" Text="*" /></TextBlock>
                <TextBox Width="200" Text="{Binding Country, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <StackPanel Margin="0,5" Orientation="Horizontal">
                <TextBlock Width="100" Text="Passport number:" />
                <TextBox Width="200" Text="{Binding PassportNumber, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <TextBlock
                Margin="0,10,0,0"
                Foreground="Red"
                Text="{Binding ErrorMessage}" />
        </StackPanel>
        <StackPanel
            Grid.Row="2"
            Margin="0,20,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Width="100"
                Margin="0,0,10,0"
                Command="{Binding SaveCommand}"
                Content="{Binding SaveButtonText}"
                IsEnabled="{Binding CanSave}" />
            <Button
                Width="100"
                Command="{Binding CancelCommand}"
                Content="Cancel" />
        </StackPanel>
    </Grid>
</Window>